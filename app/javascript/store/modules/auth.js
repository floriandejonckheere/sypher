// Initial state
const state = {
  // User object
  user: null,
  // Authentication token
  token: null,
}

// Getters
const getters = {
}

// Actions
const actions = {
}

// Mutations
const mutations = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
