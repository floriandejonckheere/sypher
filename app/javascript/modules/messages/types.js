export const actions = {
  getAll: 'messages/GET_ALL',
}

export const getters = {
  getAll: 'messages/GET_ALL',
}

export const mutations = {
  set: 'messages/SET',
}

export default {
  actions,
  getters,
  mutations,
}
