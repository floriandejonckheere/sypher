export const actions = {
  request: 'requests/REQUEST',
}

export const getters = {
  isPending: 'requests/IS_PENDING',
}

export const mutations = {
  setState: 'requests/SET_STATE',
}

export default {
  actions,
  getters,
  mutations,
}
