export const actions = {
  add: 'contacts/ADD',
}

export const getters = {
  getAll: 'contacts/GET_ALL',
}

export const mutations = {
  add: 'contacts/ADD',
}

export default {
  actions,
  getters,
  mutations,
}
